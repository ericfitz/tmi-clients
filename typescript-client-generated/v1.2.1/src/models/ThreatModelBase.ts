/* tslint:disable */
/* eslint-disable */
/**
 * TMI (Threat Modeling Improved) API
 * A RESTful API for collaborative threat modeling with full X6 graph library compatibility. This API provides schemas that align with AntV X6 cell object models for seamless integration with modern diagramming libraries. Supports OAuth 2.0 authentication with client callback integration for seamless single-page application authentication flows.  ## API Design v1.1.0  ### Authorization Model TMI uses hierarchical authorization: access control is defined at the ThreatModel level via the authorization field (readers, writers, owners). All child resources (Assets, Diagrams, Documents, Notes, Repositories, Threats) inherit permissions from their parent ThreatModel. This simplifies permission management and ensures consistent access control.  ### Bulk Operations Notes and Diagrams do not support bulk operations due to their unique creation workflows and lack of valid bulk use cases. All other resources (Threats, Assets, Documents, Repositories) support full bulk operations: POST (create), PUT (upsert), PATCH (partial update), DELETE (batch delete).  All resources support bulk metadata operations regardless of resource-level bulk support.  ### List Response Strategy - ThreatModels return summary information (TMListItem) because they contain many child objects that can be large. - Diagrams return summary information (DiagramListItem) because diagram data (cells, images) can be large. - Notes return summary information (NoteListItem) because the content field can be large. - Threats, Assets, Documents, Repositories return full schemas as they are relatively small and static.  ### PATCH Support All resources support PATCH for partial updates using JSON Patch (RFC 6902). This is particularly useful for: - Assets: Array field updates (affected_assets, trust_boundaries) ensuring no duplicates - Notes: Updating name/description without changing content field - All resources: Efficient updates without full object replacement
 *
 * The version of the OpenAPI document: 1.2.1
 * Contact: eric@tmi.dev
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from "../runtime";
import type { Authorization } from "./Authorization";
import {
  AuthorizationFromJSON,
  AuthorizationFromJSONTyped,
  AuthorizationToJSON,
  AuthorizationToJSONTyped,
} from "./Authorization";
import type { User } from "./User";
import {
  UserFromJSON,
  UserFromJSONTyped,
  UserToJSON,
  UserToJSONTyped,
} from "./User";
import type { Metadata } from "./Metadata";
import {
  MetadataFromJSON,
  MetadataFromJSONTyped,
  MetadataToJSON,
  MetadataToJSONTyped,
} from "./Metadata";

/**
 * Base schema for ThreatModel with client-writable fields
 * @export
 * @interface ThreatModelBase
 */
export interface ThreatModelBase {
  /**
   * Name of the threat model
   * @type {string}
   * @memberof ThreatModelBase
   */
  name: string;
  /**
   * Description of the threat model
   * @type {string}
   * @memberof ThreatModelBase
   */
  description?: string;
  /**
   * User who owns the threat model (can be null for orphaned models)
   * @type {User}
   * @memberof ThreatModelBase
   */
  owner: User;
  /**
   * The framework used for this threat model
   * @type {string}
   * @memberof ThreatModelBase
   */
  threat_model_framework: string;
  /**
   * List of users and their roles for this threat model
   * @type {Array<Authorization>}
   * @memberof ThreatModelBase
   */
  authorization: Array<Authorization>;
  /**
   * Key-value pairs for additional threat model metadata
   * @type {Array<Metadata>}
   * @memberof ThreatModelBase
   */
  metadata?: Array<Metadata> | null;
  /**
   * URL to an issue in an issue tracking system for this threat model
   * @type {string}
   * @memberof ThreatModelBase
   */
  issue_uri?: string;
  /**
   * Status of the threat model in the organization's threat modeling or SDLC process. Examples: "Not started", "In progress", "Review", "Approved", "Closed"
   * @type {string}
   * @memberof ThreatModelBase
   */
  status?: string | null;
  /**
   * Alternative names or identifiers for the threat model
   * @type {Set<string>}
   * @memberof ThreatModelBase
   */
  alias?: Set<string>;
  /**
   * Security reviewer assigned to this threat model. When set, the security reviewer is automatically added to the authorization list with the owner role. The security reviewer's owner role cannot be removed via authorization changes while they remain assigned as security reviewer. To change the security reviewer's authorization, first unassign them as security reviewer.
   * @type {User}
   * @memberof ThreatModelBase
   */
  security_reviewer?: User | null;
  /**
   * Optional reference to the project this threat model belongs to
   * @type {string}
   * @memberof ThreatModelBase
   */
  project_id?: string | null;
}

/**
 * Check if a given object implements the ThreatModelBase interface.
 */
export function instanceOfThreatModelBase(
  value: object,
): value is ThreatModelBase {
  if (!("name" in value) || value["name"] === undefined) return false;
  if (!("owner" in value) || value["owner"] === undefined) return false;
  if (
    !("threat_model_framework" in value) ||
    value["threat_model_framework"] === undefined
  )
    return false;
  if (!("authorization" in value) || value["authorization"] === undefined)
    return false;
  return true;
}

export function ThreatModelBaseFromJSON(json: any): ThreatModelBase {
  return ThreatModelBaseFromJSONTyped(json, false);
}

export function ThreatModelBaseFromJSONTyped(
  json: any,
  ignoreDiscriminator: boolean,
): ThreatModelBase {
  if (json == null) {
    return json;
  }
  return {
    name: json["name"],
    description: json["description"] == null ? undefined : json["description"],
    owner: UserFromJSON(json["owner"]),
    threat_model_framework: json["threat_model_framework"],
    authorization: (json["authorization"] as Array<any>).map(
      AuthorizationFromJSON,
    ),
    metadata:
      json["metadata"] == null ?
        undefined
      : (json["metadata"] as Array<any>).map(MetadataFromJSON),
    issue_uri: json["issue_uri"] == null ? undefined : json["issue_uri"],
    status: json["status"] == null ? undefined : json["status"],
    alias: json["alias"] == null ? undefined : new Set(json["alias"]),
    security_reviewer:
      json["security_reviewer"] == null ?
        undefined
      : UserFromJSON(json["security_reviewer"]),
    project_id: json["project_id"] == null ? undefined : json["project_id"],
  };
}

export function ThreatModelBaseToJSON(json: any): ThreatModelBase {
  return ThreatModelBaseToJSONTyped(json, false);
}

export function ThreatModelBaseToJSONTyped(
  value?: ThreatModelBase | null,
  ignoreDiscriminator: boolean = false,
): any {
  if (value == null) {
    return value;
  }

  return {
    name: value["name"],
    description: value["description"],
    owner: UserToJSON(value["owner"]),
    threat_model_framework: value["threat_model_framework"],
    authorization: (value["authorization"] as Array<any>).map(
      AuthorizationToJSON,
    ),
    metadata:
      value["metadata"] == null ?
        undefined
      : (value["metadata"] as Array<any>).map(MetadataToJSON),
    issue_uri: value["issue_uri"],
    status: value["status"],
    alias:
      value["alias"] == null ?
        undefined
      : Array.from(value["alias"] as Set<any>),
    security_reviewer: UserToJSON(value["security_reviewer"]),
    project_id: value["project_id"],
  };
}
