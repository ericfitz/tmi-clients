/*
 * TMI (Threat Modeling Improved) API
 * A RESTful API for collaborative threat modeling with full X6 graph library compatibility. This API provides schemas that align with AntV X6 cell object models for seamless integration with modern diagramming libraries. Supports OAuth 2.0 authentication with client callback integration for seamless single-page application authentication flows.  ## API Design v1.0.0  ### Authorization Model TMI uses hierarchical authorization: access control is defined at the ThreatModel level via the authorization field (readers, writers, owners). All child resources (Assets, Diagrams, Documents, Notes, Repositories, Threats) inherit permissions from their parent ThreatModel. This simplifies permission management and ensures consistent access control.  ### Bulk Operations Notes and Diagrams do not support bulk operations due to their unique creation workflows and lack of valid bulk use cases. All other resources (Threats, Assets, Documents, Repositories) support full bulk operations: POST (create), PUT (upsert), PATCH (partial update), DELETE (batch delete).  All resources support bulk metadata operations regardless of resource-level bulk support.  ### List Response Strategy - ThreatModels return summary information (TMListItem) because they contain many child objects that can be large. - Diagrams return summary information (DiagramListItem) because diagram data (cells, images) can be large. - Notes return summary information (NoteListItem) because the content field can be large. - Threats, Assets, Documents, Repositories return full schemas as they are relatively small and static.  ### PATCH Support All resources support PATCH for partial updates using JSON Patch (RFC 6902). This is particularly useful for: - Assets: Array field updates (affected_assets, trust_boundaries) ensuring no duplicates - Notes: Updating name/description without changing content field - All resources: Efficient updates without full object replacement 
 *
 * OpenAPI spec version: 1.0.0
 *
 * NOTE: This class is auto generated by the swagger code generator program.
 * https://github.com/swagger-api/swagger-codegen.git
 *
 * Swagger Codegen version: 3.0.75
 *
 * Do not edit the class manually.
 *
 */
import ApiClient from '../ApiClient';
import Metadata from './Metadata';

/**
 * The ThreatBase model module.
 * @module model/ThreatBase
 * @version 1.0.0
 */
export default class ThreatBase {
  /**
   * Constructs a new <code>ThreatBase</code>.
   * Base schema for Threat with client-writable fields
   * @alias module:model/ThreatBase
   * @class
   * @param name {String} Name of the threat
   * @param severity {module:model/ThreatBase.SeverityEnum} Severity level of the threat
   * @param priority {String} Priority level for addressing the threat
   * @param mitigated {Boolean} Whether the threat has been mitigated
   * @param status {String} Current status of the threat
   * @param threatType {String} Type or category of the threat
   */
  constructor(name, severity, priority, mitigated, status, threatType) {
    this.name = name;
    this.severity = severity;
    this.priority = priority;
    this.mitigated = mitigated;
    this.status = status;
    this.threatType = threatType;
  }

  /**
   * Constructs a <code>ThreatBase</code> from a plain JavaScript object, optionally creating a new instance.
   * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
   * @param {Object} data The plain JavaScript object bearing properties of interest.
   * @param {module:model/ThreatBase} obj Optional instance to populate.
   * @return {module:model/ThreatBase} The populated <code>ThreatBase</code> instance.
   */
  static constructFromObject(data, obj) {
    if (data) {
      obj = obj || new ThreatBase();
      if (data.hasOwnProperty('name'))
        obj.name = ApiClient.convertToType(data['name'], 'String');
      if (data.hasOwnProperty('description'))
        obj.description = ApiClient.convertToType(data['description'], 'String');
      if (data.hasOwnProperty('mitigation'))
        obj.mitigation = ApiClient.convertToType(data['mitigation'], 'String');
      if (data.hasOwnProperty('diagram_id'))
        obj.diagramId = ApiClient.convertToType(data['diagram_id'], 'String');
      if (data.hasOwnProperty('cell_id'))
        obj.cellId = ApiClient.convertToType(data['cell_id'], 'String');
      if (data.hasOwnProperty('severity'))
        obj.severity = ApiClient.convertToType(data['severity'], 'String');
      if (data.hasOwnProperty('score'))
        obj.score = ApiClient.convertToType(data['score'], 'Number');
      if (data.hasOwnProperty('priority'))
        obj.priority = ApiClient.convertToType(data['priority'], 'String');
      if (data.hasOwnProperty('mitigated'))
        obj.mitigated = ApiClient.convertToType(data['mitigated'], 'Boolean');
      if (data.hasOwnProperty('status'))
        obj.status = ApiClient.convertToType(data['status'], 'String');
      if (data.hasOwnProperty('threat_type'))
        obj.threatType = ApiClient.convertToType(data['threat_type'], 'String');
      if (data.hasOwnProperty('metadata'))
        obj.metadata = ApiClient.convertToType(data['metadata'], [Metadata]);
      if (data.hasOwnProperty('issue_uri'))
        obj.issueUri = ApiClient.convertToType(data['issue_uri'], 'String');
      if (data.hasOwnProperty('asset_id'))
        obj.assetId = ApiClient.convertToType(data['asset_id'], 'String');
    }
    return obj;
  }
}

/**
 * Name of the threat
 * @member {String} name
 */
ThreatBase.prototype.name = undefined;

/**
 * Description of the threat and risk to the organization
 * @member {String} description
 */
ThreatBase.prototype.description = undefined;

/**
 * Recommended or planned mitigation(s) for the threat
 * @member {String} mitigation
 */
ThreatBase.prototype.mitigation = undefined;

/**
 * Unique identifier of the associated diagram (if applicable) (UUID)
 * @member {String} diagramId
 */
ThreatBase.prototype.diagramId = undefined;

/**
 * Unique identifier of the associated cell (if applicable) (UUID)
 * @member {String} cellId
 */
ThreatBase.prototype.cellId = undefined;

/**
 * Allowed values for the <code>severity</code> property.
 * @enum {String}
 * @readonly
 */
ThreatBase.SeverityEnum = {
  /**
   * value: "Unknown"
   * @const
   */
  unknown: "Unknown",

  /**
   * value: "None"
   * @const
   */
  none: "None",

  /**
   * value: "Low"
   * @const
   */
  low: "Low",

  /**
   * value: "Medium"
   * @const
   */
  medium: "Medium",

  /**
   * value: "High"
   * @const
   */
  high: "High",

  /**
   * value: "Critical"
   * @const
   */
  critical: "Critical"
};
/**
 * Severity level of the threat
 * @member {module:model/ThreatBase.SeverityEnum} severity
 */
ThreatBase.prototype.severity = undefined;

/**
 * Numeric score representing the risk or impact of the threat
 * @member {Number} score
 */
ThreatBase.prototype.score = undefined;

/**
 * Priority level for addressing the threat
 * @member {String} priority
 */
ThreatBase.prototype.priority = undefined;

/**
 * Whether the threat has been mitigated
 * @member {Boolean} mitigated
 */
ThreatBase.prototype.mitigated = undefined;

/**
 * Current status of the threat
 * @member {String} status
 */
ThreatBase.prototype.status = undefined;

/**
 * Type or category of the threat
 * @member {String} threatType
 */
ThreatBase.prototype.threatType = undefined;

/**
 * Key-value pairs for additional threat metadata
 * @member {Array.<module:model/Metadata>} metadata
 */
ThreatBase.prototype.metadata = undefined;

/**
 * URL to an issue in an issue tracking system for this threat
 * @member {String} issueUri
 */
ThreatBase.prototype.issueUri = undefined;

/**
 * Unique identifier of the associated asset (if applicable) (UUID)
 * @member {String} assetId
 */
ThreatBase.prototype.assetId = undefined;

