# coding: utf-8

"""
TMI (Threat Modeling Improved) API

A RESTful API for collaborative threat modeling with full X6 graph library compatibility. This API provides schemas that align with AntV X6 cell object models for seamless integration with modern diagramming libraries. Supports OAuth 2.0 authentication with client callback integration for seamless single-page application authentication flows.  ## API Design v1.1.0  ### Authorization Model TMI uses hierarchical authorization: access control is defined at the ThreatModel level via the authorization field (readers, writers, owners). All child resources (Assets, Diagrams, Documents, Notes, Repositories, Threats) inherit permissions from their parent ThreatModel. This simplifies permission management and ensures consistent access control.  ### Bulk Operations Notes and Diagrams do not support bulk operations due to their unique creation workflows and lack of valid bulk use cases. All other resources (Threats, Assets, Documents, Repositories) support full bulk operations: POST (create), PUT (upsert), PATCH (partial update), DELETE (batch delete).  All resources support bulk metadata operations regardless of resource-level bulk support.  ### List Response Strategy - ThreatModels return summary information (TMListItem) because they contain many child objects that can be large. - Diagrams return summary information (DiagramListItem) because diagram data (cells, images) can be large. - Notes return summary information (NoteListItem) because the content field can be large. - Threats, Assets, Documents, Repositories return full schemas as they are relatively small and static.  ### PATCH Support All resources support PATCH for partial updates using JSON Patch (RFC 6902). This is particularly useful for: - Assets: Array field updates (affected_assets, trust_boundaries) ensuring no duplicates - Notes: Updating name/description without changing content field - All resources: Efficient updates without full object replacement

The version of the OpenAPI document: 1.3.0
Contact: eric@tmi.dev
Generated by OpenAPI Generator (https://openapi-generator.tech)

Do not edit the class manually.
"""  # noqa: E501

from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from datetime import datetime
from pydantic import (
    BaseModel,
    ConfigDict,
    Field,
    StrictBool,
    StrictStr,
    field_validator,
)
from typing import Any, ClassVar, Dict, List, Optional
from typing_extensions import Annotated
from uuid import UUID
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python


class SystemSetting(BaseModel):
    """
    A system-wide configuration setting
    """  # noqa: E501

    key: Annotated[str, Field(strict=True, max_length=256)] = Field(
        description="Unique setting identifier using dot notation (e.g., rate_limit.requests_per_minute)"
    )
    value: Annotated[str, Field(strict=True, max_length=4000)] = Field(
        description="Setting value as a string (interpreted based on type)"
    )
    type: StrictStr = Field(description="Data type of the setting value")
    description: Optional[Annotated[str, Field(strict=True, max_length=2048)]] = Field(
        default=None, description="Human-readable description of the setting"
    )
    modified_at: Optional[datetime] = Field(
        default=None, description="When the setting was last modified"
    )
    modified_by: Optional[UUID] = Field(
        default=None, description="UUID of the user who last modified the setting"
    )
    source: Optional[StrictStr] = Field(
        default=None,
        description="Where the effective value of this setting comes from. Server-managed, not writable.",
    )
    read_only: Optional[StrictBool] = Field(
        default=None,
        description="Whether this setting can be modified via the API. True when source is not database.",
    )
    __properties: ClassVar[List[str]] = [
        "key",
        "value",
        "type",
        "description",
        "modified_at",
        "modified_by",
        "source",
        "read_only",
    ]

    @field_validator("key")
    def key_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        value = value.isoformat() if hasattr(value, "isoformat") else str(value)
        if not re.match(r"^[a-z][a-z0-9_.]*$", value):
            raise ValueError(
                r"must validate the regular expression /^[a-z][a-z0-9_.]*$/"
            )
        return value

    @field_validator("type")
    def type_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(["string", "int", "bool", "json"]):
            raise ValueError(
                "must be one of enum values ('string', 'int', 'bool', 'json')"
            )
        return value

    @field_validator("source")
    def source_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(["database", "config", "environment", "vault"]):
            raise ValueError(
                "must be one of enum values ('database', 'config', 'environment', 'vault')"
            )
        return value

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )

    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of SystemSetting from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        * OpenAPI `readOnly` fields are excluded.
        * OpenAPI `readOnly` fields are excluded.
        """
        excluded_fields: Set[str] = set(
            [
                "source",
                "read_only",
            ]
        )

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of SystemSetting from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate(
            {
                "key": obj.get("key"),
                "value": obj.get("value"),
                "type": obj.get("type"),
                "description": obj.get("description"),
                "modified_at": obj.get("modified_at"),
                "modified_by": obj.get("modified_by"),
                "source": obj.get("source"),
                "read_only": obj.get("read_only"),
            }
        )
        return _obj
