# coding: utf-8

"""
    TMI (Threat Modeling Improved) API

    A RESTful API for collaborative threat modeling with full X6 graph library compatibility. This API provides schemas that align with AntV X6 cell object models for seamless integration with modern diagramming libraries. Supports OAuth 2.0 authentication with client callback integration for seamless single-page application authentication flows.  ## API Design v1.1.0  ### Authorization Model TMI uses hierarchical authorization: access control is defined at the ThreatModel level via the authorization field (readers, writers, owners). All child resources (Assets, Diagrams, Documents, Notes, Repositories, Threats) inherit permissions from their parent ThreatModel. This simplifies permission management and ensures consistent access control.  ### Bulk Operations Notes and Diagrams do not support bulk operations due to their unique creation workflows and lack of valid bulk use cases. All other resources (Threats, Assets, Documents, Repositories) support full bulk operations: POST (create), PUT (upsert), PATCH (partial update), DELETE (batch delete).  All resources support bulk metadata operations regardless of resource-level bulk support.  ### List Response Strategy - ThreatModels return summary information (TMListItem) because they contain many child objects that can be large. - Diagrams return summary information (DiagramListItem) because diagram data (cells, images) can be large. - Notes return summary information (NoteListItem) because the content field can be large. - Threats, Assets, Documents, Repositories return full schemas as they are relatively small and static.  ### PATCH Support All resources support PATCH for partial updates using JSON Patch (RFC 6902). This is particularly useful for: - Assets: Array field updates (affected_assets, trust_boundaries) ensuring no duplicates - Notes: Updating name/description without changing content field - All resources: Efficient updates without full object replacement 

    The version of the OpenAPI document: 1.5.0
    Contact: api@tmi.local.dev
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from datetime import datetime
from pydantic import BaseModel, ConfigDict, Field, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing_extensions import Annotated
from uuid import UUID
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class ContentFeedback(BaseModel):
    """
    ContentFeedback
    """ # noqa: E501
    sentiment: StrictStr
    target_type: StrictStr = Field(description="Type of artifact the feedback targets")
    target_id: UUID = Field(description="ID of the targeted artifact (note/diagram/threat). For threat_classification, the threat ID.")
    target_field: Optional[Annotated[str, Field(strict=True, max_length=64)]] = Field(default=None, description="Required iff target_type is 'threat_classification'; identifies the field of the threat (e.g. 'cwe', 'severity')")
    verbatim: Optional[Annotated[str, Field(strict=True, max_length=2048)]] = None
    false_positive_reason: Optional[StrictStr] = Field(default=None, description="Allowed only when sentiment='down' and target_type='threat'")
    false_positive_subreason: Optional[StrictStr] = Field(default=None, description="Allowed only when false_positive_reason has subreasons; must be a valid subreason for the chosen reason")
    client_id: Annotated[str, Field(strict=True, max_length=32)]
    client_version: Optional[Annotated[str, Field(strict=True, max_length=32)]] = None
    screenshot: Optional[Annotated[str, Field(strict=True, max_length=2000000)]] = Field(default=None, description="Optional viewport screenshot captured by the client at submission time. Data URL form (e.g. `data:image/jpeg;base64,...`). Used as support context.")
    id: UUID
    threat_model_id: UUID
    created_by: UUID
    created_at: datetime
    __properties: ClassVar[List[str]] = ["sentiment", "target_type", "target_id", "target_field", "verbatim", "false_positive_reason", "false_positive_subreason", "client_id", "client_version", "screenshot", "id", "threat_model_id", "created_by", "created_at"]

    @field_validator('sentiment')
    def sentiment_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['up', 'down']):
            raise ValueError("must be one of enum values ('up', 'down')")
        return value

    @field_validator('target_type')
    def target_type_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['note', 'diagram', 'threat', 'threat_classification']):
            raise ValueError("must be one of enum values ('note', 'diagram', 'threat', 'threat_classification')")
        return value

    @field_validator('false_positive_reason')
    def false_positive_reason_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['detection_misfired', 'real_but_mitigated', 'real_but_not_exploitable', 'out_of_scope', 'intended_behavior', 'duplicate', 'already_remediated', 'detection_rule_flawed']):
            raise ValueError("must be one of enum values ('detection_misfired', 'real_but_mitigated', 'real_but_not_exploitable', 'out_of_scope', 'intended_behavior', 'duplicate', 'already_remediated', 'detection_rule_flawed')")
        return value

    @field_validator('false_positive_subreason')
    def false_positive_subreason_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['code_does_not_exist', 'trigger_conditions_not_met', 'component_outside_threat_model', 'sanctioned_by_design', 'not_a_real_risk', 'needs_tuning']):
            raise ValueError("must be one of enum values ('code_does_not_exist', 'trigger_conditions_not_met', 'component_outside_threat_model', 'sanctioned_by_design', 'not_a_real_risk', 'needs_tuning')")
        return value

    @field_validator('client_id')
    def client_id_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        value = value.isoformat() if hasattr(value, 'isoformat') else str(value)
        if not isinstance(value, str):
            value = str(value)

        if not re.match(r"^[a-z][a-z0-9_-]{0,31}$", value):
            raise ValueError(r"must validate the regular expression /^[a-z][a-z0-9_-]{0,31}$/")
        return value

    @field_validator('screenshot')
    def screenshot_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        value = value.isoformat() if hasattr(value, 'isoformat') else str(value)
        if not isinstance(value, str):
            value = str(value)

        if not re.match(r"^data:image\/(jpeg|png|webp);base64,[A-Za-z0-9+\/=]+$", value):
            raise ValueError(r"must validate the regular expression /^data:image\/(jpeg|png|webp);base64,[A-Za-z0-9+\/=]+$/")
        return value

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of ContentFeedback from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of ContentFeedback from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "sentiment": obj.get("sentiment"),
            "target_type": obj.get("target_type"),
            "target_id": obj.get("target_id"),
            "target_field": obj.get("target_field"),
            "verbatim": obj.get("verbatim"),
            "false_positive_reason": obj.get("false_positive_reason"),
            "false_positive_subreason": obj.get("false_positive_subreason"),
            "client_id": obj.get("client_id"),
            "client_version": obj.get("client_version"),
            "screenshot": obj.get("screenshot"),
            "id": obj.get("id"),
            "threat_model_id": obj.get("threat_model_id"),
            "created_by": obj.get("created_by"),
            "created_at": obj.get("created_at")
        })
        return _obj


